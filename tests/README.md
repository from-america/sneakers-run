# Historical visual evidence

These images and the art-direction comparison document the pre-rebuild game.
They are preserved for visual comparison, not used as current acceptance tests.
The old engines, tests, and metadata-only route tools were retired in version 2.

Current executable tests and reviewed captures are in `../game-tests/`.
The replacement runtime is `../game/`, loaded directly by `../index.html`.
